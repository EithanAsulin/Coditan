# CodiPilot
CodiPilot generates simple bar or line charts as an SVG file using only the Python standard library.

## Usage
To use CodiPilot you must use in this format

```action
<custom_codipilot>
values="[10, 20, 15, 30]"
labels="['A', 'B', 'C', 'D']"
chart_type="bar"
</custom_codipilot>
```